# Home Assistant loads the integration from custom_components/.
# User-facing release version: custom_components/zigbee_manager/manifest.json

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"
